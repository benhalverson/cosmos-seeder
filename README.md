# USAGE

`python cli.py seed --cosmos-url "<COSMOS_URL>" --db-name "<DATABASE_NAME>" --container-name "<CONTAINER_NAME>" --data-file "data.json"`

If you want to trigger the function after seeding the data, you can use the following command:
`python cli.py trigger --function-url "<FUNCTION_URL>"`